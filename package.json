{
  "name": "quizz-project-nexa",
  "version": "0.1.0",
  "engines": {
    "node": "22.13.0",
    "npm": "11.0.0"
  },
  "scripts": {
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "setup": "npm install && npm install --prefix ./client && npm install --prefix ./server && cd ..",
    "build": "npm run setup && cd client && npm run build && cd ../server && npm run build && cd ..",
    "start": "cd server && npm run start",
    "prod": "npm run build && npm run start"
  },
  "author": "Alexandre",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
